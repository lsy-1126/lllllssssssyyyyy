# 任务一：生活中的机器学习场景识别
1. ## 手机人脸解锁、相册自动分类
    - 场景：人脸解锁、相册按人物/场景聚类、照片美化。
    - 可能模型：人脸检测用 MTCNN、RetinaFace、YOLO；人脸识别用 FaceNet、ArcFace、CosFace；图像分类用 ResNet、MobileNet、EfficientNet；聚类可用 k-means、DBSCAN。
2. ## 语音助手、语音输入法
    - 场景：Siri、小爱同学、语音转文字、语音唤醒。
    - 可能模型：语音识别早期用 GMM-HMM，现在常用 CTC、RNN-T、Conformer、Whisper；语音合成用 Tacotron、FastSpeech、VITS、HiFi-GAN；意图理解用 BERT、RoBERTa 或大语言模型。
3. ## 输入法联想、纠错、下一词预测
    - 场景：手机输入法候选词、自动纠错、整句补全。
    - 可能模型：早期 N-gram，后来用 RNN/LSTM、Transformer、BERT、GPT 类模型。端侧常用 TinyBERT、蒸馏小模型。
4. ## 机器翻译、拍照翻译
    - 场景：网页翻译、出国旅游拍照翻译。
    - 可能模型：Seq2Seq + Attention、Transformer、mBART、mT5、多语言大模型；拍照翻译还会叠加 OCR 模型。
5. ## 短视频、电商、新闻推荐
    - 场景：抖音/淘宝/新闻 App 的“猜你喜欢”。
    - 可能模型：传统协同过滤、矩阵分解；排序常用 LR、GBDT、XGBoost、LightGBM、FM/FFM；深度学习常用 Wide&Deep、DeepFM、DIN、双塔模型、Transformer、GNN、强化学习。典型流程是“召回—粗排—精排—重排”。
6. ## 自动驾驶、辅助驾驶
    - 场景：车道保持、自动泊车、障碍物识别。
    - 可能模型：CNN、ResNet、YOLO 做目标检测；PointNet、PointPillars 处理激光雷达点云；BEVFormer、Transformer 做多传感器融合；LSTM/Transformer 做轨迹预测；决策控制可能用强化学习 PPO、DQN 或模仿学习。
7. ## 智能客服、聊天机器人
    - 场景：电商客服、银行问答、AI 助手。
    - 可能模型：检索式用 TF-IDF、BM25、向量检索；生成式用 Seq2Seq、BERT、GPT 类大模型；现在常结合 RAG：大模型 + 向量数据库。


# 任务二：机器学习核心概念探究
1. ## 数据与数据处理
    - ## 什么是被处理过的数据？
      指经过清洗、转换、规整和特征工程后，干净、完整、格式统一、可直接输入模型训练的数据。例如：缺失值已填充、异常值已处理、类别已编码、数值已归一化。

    - ## 为什么要做数据处理？
      原始数据常有噪声、缺失、重复、格式不一致，直接训练会导致模型效果差甚至无法训练。数据处理能提高数据质量、统一格式、满足模型输入要求，从而提升模型精度、稳定性和泛化能力。

    - ## 常见数据处理手段：
         > 数据清洗：处理缺失值、异常值、重复值、噪声。
         
         > 数据集成：合并多来源数据，统一字段和格式。
         
         > 数据变换：归一化、标准化、编码、离散化、对数变换。
         
         > 数据规约：降维、采样、特征压缩。
         
         > 特征工程：特征提取、选择、构造。
         
         > 数据增强：旋转、裁剪、加噪、生成新样本。
         
         > 数据划分：分为训练集、验证集、测试集。
2. ## 数据集划分逻辑
    1. ### 数据集
       > 训练集：用来训练模型参数，让模型学习规律。
       
       > 验证集：用来调超参数、选模型、早停，不参与参数训练。
       
       > 测试集：只在最后评估一次，检验模型对没见过的新数据的泛化能力。

    3. ### 原因
       - 三者职责分开，避免信息泄漏和过拟合。

       - 若用测试集调参，测试结果会偏乐观，不能反映真实泛化能力。

       - 若没有验证集，就难以客观选模型和调参。
 
       - 测试集模拟未来新数据，给出最终客观性能估计。
3. ## 拟合问题：过拟合与欠拟合
    1. ### 概念
          - 欠拟合：高偏差，模型太弱。
          - 过拟合：高方差，模型太敏感。
    2. ### 原因&解法
          - 过拟合
            > 常见原因：
            > > 模型过于复杂，参数太多。
            
            > > 训练数据太少，或噪声、异常值太多。
            
            > > 特征过多，包含大量无关特征。
            
            > > 训练轮次过多，训练过度。
            
            > 解决方法：
            > > 增加训练数据，或做数据增强。
            
            > > 简化模型，降低复杂度。
            
            > > 做特征选择、降维，去掉无关特征。
            
            > > 清洗噪声和异常值。
            
            > > 使用交叉验证、集成学习，如随机森林、XGBoost。
            
            > > 调整模型容量，如减少决策树深度、减少神经网络层数。



